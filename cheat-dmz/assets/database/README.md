# CHeaT Defense Database

Este diretório contém os bancos de dados JSON de técnicas e templates do CHeaT, extraídos do repositório original. Eles são usados pela CLI `cheat` para gerar e plantar defesas nos assets da DMZ.

Os arquivos são copiados automaticamente durante a instalação pelo script `Install-CHeaTDefense.ps1`.
